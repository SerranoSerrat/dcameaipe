% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dcame_aipe.R
\name{dcame_aipe}
\alias{dcame_aipe}
\title{Estimate DCAME and AIPE for interactive treatment effects}
\usage{
dcame_aipe(
  data,
  Y,
  D,
  X,
  Z = NULL,
  model = "basis",
  estimand,
  vce = "robust",
  cluster = NULL,
  wgt = NULL,
  Z_interact = FALSE,
  Z_FE = NULL,
  Z_FE_absorb = FALSE,
  user_spec_0 = NULL,
  user_spec_1 = NULL,
  user_spec_2 = NULL,
  user_spec_3 = NULL,
  user_spec_4 = NULL,
  compare = FALSE,
  selection = "CV",
  inference = "crossfit",
  B_boot = 500,
  n_grid = 1000,
  level_pv = 0.84,
  level_est = 0.95,
  n_parts = 3,
  smooth = "REML",
  k_s = NULL,
  k_t = NULL,
  inter_gam = "ti",
  max_poly = 5,
  max_knots = 4,
  joint_select = FALSE,
  hist_pv = "rug",
  plot_came_all = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame} containing all variables referenced by name.}

\item{Y}{Character. Name of the outcome variable.}

\item{D}{Character. Name of the (continuous) treatment variable.}

\item{X}{Character. Name of the moderator. Can be binary (0/1), numeric
continuous (auto-binned via \code{n_parts}), or a two-level
character/factor (auto-coded to 0/1).}

\item{Z}{Character vector of control variable names. Default \code{NULL}.}

\item{model}{Either \code{"basis"} (default, polynomial/B-spline) or \code{"GAM"}
(penalised splines via \code{mgcv}).}

\item{estimand}{\code{"AIPE"} or \code{"DCAME"}. Must be specified.}

\item{vce}{\code{"robust"} (default, HC1) or \code{"cluster"} (requires \code{cluster}).
Note: for \code{model = "GAM"} with \code{inference = "regular"}, the analytic
mgcv posterior is used regardless of \code{vce}; cluster SEs require
\code{inference = "bootstrap"}.}

\item{cluster}{Character. Name of the clustering variable (used when
\code{vce = "cluster"} for the basis path, and for cluster bootstrap in both
paths).}

\item{wgt}{Character. Optional name of a survey-weights variable. Honoured
throughout the basis path and the GAM path (fit, prediction Z-bar,
FE modal-level selection, AIPE/CAME averaging, bootstrap resampling).}

\item{Z_interact}{Logical. If \code{TRUE}, controls in \code{Z} are interacted with
the K-group factor (i.e., \eqn{Z \cdot G_k}) in both the basis and GAM
paths. For binary X this is equivalent to \eqn{Z \cdot X}.}

\item{Z_FE}{Character vector of fixed-effect variable names (additive dummies).}

\item{Z_FE_absorb}{Logical. If \code{TRUE}, FE in \code{Z_FE} are absorbed via
\code{fixest::feols} instead of expanded into dummies. Use this when one or
more FE variables has high cardinality (school, person, fine grid) and
would blow up memory. Requires the \code{fixest} package. \strong{Only supported in
the basis path}; ignored (with a warning) under \code{model = "GAM"}.
Under analytic inference (\code{"regular"} / \code{"crossfit"}), predicted-value
CIs reflect within-FE-group uncertainty only; use \code{inference = "bootstrap"}
for PV CIs comparable to \code{Z_FE_absorb = FALSE}. Default \code{FALSE}.}

\item{user_spec_0, user_spec_1, user_spec_2, user_spec_3, user_spec_4}{Optional user-specified functional forms, one per group. Numeric =>
polynomial degree; character (e.g. \code{"3,3"}) => B-spline with knots,degree.
For binary X, only \code{user_spec_0}/\code{user_spec_1} are used. For continuous X
with \code{n_parts = K}, supply the first K (\verb{user_spec_0..user_spec_\{K-1\}}).
Either supply ALL K or leave ALL NULL (no partial pre-specification).}

\item{compare}{\code{FALSE} (default), \code{"LINEAR"}, \code{"DCAME"}, \code{"AIPE"}, or \code{"ALL"}.
Adds the requested side-by-side comparison(s). The \code{"LINEAR"} bar is the
joint linear-per-group D-CAME contrast (slope at group K-1 minus slope at
group 0), fitted as \code{Y ~ D * factor(g) + X * factor(g) + Z + FE} on the
same trimmed sample. It is exactly the basis estimate when
\verb{user_spec_0 = ... = user_spec_\{K-1\} = 1}.}

\item{selection}{Model selection criterion for the basis path:
\code{"CV"} (default, 10-fold per-group CV), \code{"BIC"} (per-group), or
\code{"CV_targeted"} (10-fold CV with AIPE-targeted density-ratio weights
\code{f_pool(D)/f_own_k(D)}; only meaningful when \code{estimand = "AIPE"} —
falls back to plain CV with a warning otherwise).}

\item{inference}{\code{"crossfit"} (default), \code{"bootstrap"}, or \code{"regular"}.
GAM path uses analytic (mgcv) or bootstrap inference.}

\item{B_boot}{Number of bootstrap replicates (default 500).}

\item{n_grid}{Number of evaluation points on the D-grid (default 1000).}

\item{level_pv}{Confidence level for predicted-value bands (default 0.84).}

\item{level_est}{Confidence level for estimands (default 0.95).}

\item{n_parts}{Number of bins for continuous X (2, 3, 4, or 5; default 3).}

\item{smooth}{GAM smoothness selection: \code{"REML"} (default) or \code{"GCV"}.}

\item{k_s, k_t}{GAM basis dimensions for \code{s()} and tensor \code{te()/ti()} smooths.}

\item{inter_gam}{GAM continuous-X interaction form: \code{"ti"} (default) or \code{"te"}.}

\item{max_poly}{Highest polynomial degree to search (basis path).}

\item{max_knots}{Maximum number of interior knots to search (basis path).}

\item{joint_select}{Logical. Joint vs. independent CV selection across X groups.}

\item{hist_pv}{Rug/percentile annotation on the predicted-values plot:
\code{"rug"} (default), \code{"perc"}, or \code{"rug_1000"}.}

\item{plot_came_all}{Logical. If \code{TRUE} and X is continuous, also produce
\code{plot_came_all} showing CAME at every level (\code{n_parts} groups), with CIs.
The K per-group CAMEs come directly from the joint K-group model
(basis path) or from evaluating the fitted GAM at K quantile midpoints
(GAM path). Default \code{FALSE}. Ignored when X is binary.}

\item{verbose}{Logical. Print progress messages.}
}
\value{
An object of class \code{dcame_aipe}. Key fields:
\describe{
\item{\code{aipe}, \code{se_aipe}, \code{ci_aipe}}{Average Interactive Partial Effect.}
\item{\code{dcame}, \code{se_dcame}, \code{ci_dcame}}{Difference in CAMEs.}
\item{\code{came0}, \code{se_came0}, \code{came1}, \code{se_came1}}{CAME at low / high
moderator group endpoints.}
\item{\code{linear_dx}, \code{se_linear_dx}}{Linear D-CAME contrast from the joint
linear-per-group fit (when \code{compare} includes \code{"LINEAR"}/\code{"ALL"}).}
\item{\code{d_grid}, \code{yhat_X0}, \code{yhat_X1}, \code{se_yhat_X0}, \code{se_yhat_X1}}{
Predicted-value curves at low/high moderator levels with bands.}
\item{\code{slope_grid_X0}, \code{slope_grid_X1}, \code{ipe_grid}, \code{se_ipe_grid}}{
Pointwise slope and IPE curves along D.}
\item{\code{came_all}}{Data frame of per-group CAMEs when \code{plot_came_all = TRUE}.}
\item{\code{common_support}, \code{n_trimmed}, \code{n_used}}{Common-support trim info.}
\item{\code{K}, \code{tercile_cuts}, \code{Z_FE_absorb}, \code{compare}}{Reproducibility metadata.}
\item{\code{plot}, \code{plot_predicted}, \code{plot_ipe}, \code{plot_coef}, \code{plot_came_all}}{
Ready-to-print \code{ggplot} / \code{gtable} objects.}
\item{\code{fit}, \code{vcov}}{Underlying fit object (when stored) and vcov.}
}
}
\description{
Implements the empirical approach from Serrano-Serrat,
\emph{The Pitfalls of Assuming Linear Treatment Effects in Interaction Tests},
\emph{Sociological Science}, 2026.
Fits the K-group joint model
\deqn{Y_i = \sum_{k=0}^{K-1} \big\{ \mu_k + f_k(D_i) + \alpha_k X_i \big\} G_{k,i}
             + \delta' Z_i + \mathrm{FE} + \varepsilon_i,}
where \eqn{G_{k,i} = \mathbf{1}\{g(X_i) = k\}} is an indicator for the
\eqn{k}-th moderator group and \eqn{f_k(\cdot)} is a group-specific
polynomial or B-spline (basis path) / penalised smooth (GAM path) of \eqn{D}.
For binary \eqn{X}, \eqn{K = 2} and the \eqn{\alpha_k X G_k} term is
collinear with \eqn{G_k}, so it is omitted. For continuous \eqn{X},
\eqn{K = n\_parts} groups defined by quantile binning, and middle groups
are kept (they contribute to fitting nuisance parameters but the headline
D-CAME/AIPE contrasts use only \eqn{G_0} vs. \eqn{G_{K-1}}).
}
\details{
Returns the Average Interactive Partial Effect (AIPE) and/or the
Difference in Conditional Average Marginal Effects (D-CAME), with
standard errors, confidence intervals, and diagnostic plots.
}
\examples{
\dontrun{
# Basic usage with a simulated data frame
res <- dcame_aipe(data = mydata, Y = "Y", D = "D", X = "X",
                  estimand = "AIPE", compare = "ALL")
summary(res)
res$plot
}

}
